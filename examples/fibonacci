fib(n) {
    var a, b, i, t;
    a = 0;
    b = 1;
    i = 0;

    while (n > i) {
		output i;
        t = a + b;
        a = b;
        b = t;
        i = i + 1;
    }

    return a;
}

main() {
	var n, x;
	input n;
    x = fib(n);
    return x;
}
